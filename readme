This is a simple console-based customer billing system written in C. It allows users to add customers and display existing customer records from a file named `customer_data.txt`.
1. Compile the code using a C compiler (e.g., GCC).
2. Run the executable.
3. Follow the menu prompts to add customers or display existing records.
